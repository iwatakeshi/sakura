// /**
//  * Adds a list of elements before the specified index in the array.
//  *
//  * __Note:__ This function does not mutate the original array.
//  * @param array - The array.
//  * @param items - The elements to push.
//  * @param index - The index to push the element before.
//  */
// export function concatAt<T>(array: T[], index: number, ...items: T[]): T[] {
//   return [...array.slice(0, index), ...items, ...array.slice(index)]
// }
